<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weibo.demo.mapper.userMapper">
    <select id="selectUserByUserNameAndPassword" resultType="java.lang.Long">
        SELECT id
        FROM user
        WHERE username=#{username} AND password=#{password}
    </select>
    <insert id="insertUser">
        INSERT INTO user(
        username,password,nickname,create_time
        )
        VALUES(
        #{username},#{password},#{nickname},#{createTime}
        )
    </insert>
    <select id="selectUser" resultType="com.weibo.demo.pojo.vo.UserVo">
        SELECT id,username
        FROM user
        WHERE username=#{username}
        AND password=#{password}
    </select>

</mapper>
