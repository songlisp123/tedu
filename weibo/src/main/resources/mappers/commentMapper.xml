<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.weibo.demo.mapper.commentMapper">
    <insert id="insertComment">
        INSERT INTO comment(content,create_time,user_id,weibo_id)
        VALUES(#{content},#{createTime},#{userId},#{weiBoId})
    </insert>
    <select id="selectAllcomments" resultType="com.weibo.demo.pojo.vo.CommentVO">
        SELECT c.id,c.content,c.create_time as createTime,u.nickname
        FROM comment as c
        JOIN user as u ON c.user_id=u.id
        WHERE c.weibo_id=#{id}
    </select>
</mapper>
